// Legacy import path. The whole site now uses the new dark + saffron SiteFooter
// from the homepage redesign. Re-exporting keeps every page that already imports
// `@/components/Footer` working without edits.
export { default } from "./home/SiteFooter";
