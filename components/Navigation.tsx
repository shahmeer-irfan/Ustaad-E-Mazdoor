"use client";

// Legacy import path. The whole site now uses the new dark + saffron Navbar
// from the homepage redesign. Re-exporting keeps every page that already imports
// `@/components/Navigation` working without edits.
export { default } from "./home/Navbar";
